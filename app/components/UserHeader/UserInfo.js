import styled from 'styled-components';

const UserInfo = styled.span`
    display: flex;
    flex-direction: row;
    align-items: center;
    cursor: pointer;
    padding: 0 12px;
    &:hover {
        background: #e6f7ff;
    }
`;

export default UserInfo;
