import styled from 'styled-components';

const Pre = styled.pre`
    margin: 4px auto;
    & > em {
        background-color: rgba(27, 31, 35, 0.05);
        padding: 2px 6px;
        border-radius: 4px;
        font-size: 14px;
        font-style: normal;
    }
`;

export default Pre;
